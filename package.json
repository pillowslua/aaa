{
  "name": "dstat-dashboard",
  "version": "1.0.0",
  "description": "Multi-server DSTAT monitoring dashboard",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
